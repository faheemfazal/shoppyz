<!-- <!DOCTYPE html>
<html lang="en">
<head> -->
<!-- <meta charset="utf-8"> -->
<!--  This file has been downloaded from bootdey.com @bootdey on twitter -->
<!--  All snippets are MIT license http://bootdey.com/license -->

<!-- <meta name="viewport" content="width=device-width, initial-scale=1"> -->
<!-- <script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet">
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js"></script> -->
<!-- </head> -->
<%- include('userpartials/userheader.ejs') %>
    <!-- <body> -->

    <div class="container px-4 mt-4 pt-5">
        <!-- Account page navigation-->
        <nav class="nav nav-borders" style="margin-top: 50px;">
            <!-- <a class="nav-link active ms-0" href="https://www.bootdey.com/snippets/view/bs5-edit-profile-account-details" target="__blank">Profile</a> -->
            <a class="nav-link" href="#" style="color: black;" >Your Address</a>
            <a class="nav-link" style="color: black;"  href="/Orders" >Your Orders</a>

        </nav>
        <hr class="mt-0 mb-4">

        <div class="row">
            <div class="col-lg-3">
                <!-- Profile picture card-->
                <div class="card mb-4 mb-xl-0 mt-2">
                    <div class="card-header">Profile Picture</div>
                    <div class="card-body text-center">
                        <!-- Profile picture image-->
                        <% for( let i=0; i < 1; i++ ) { %>
                            <% if (findAddress.Address[0].imageUrl[0].filename) { %>
                                <img class="img-account-profile rounded-circle mb-2" style="    width: 97%;
                                height: 97%;"
                                    src="/images/<%= findAddress.Address[0].imageUrl[0].filename %> " alt="">
                                <% }else{ %>
                                    <img class="img-account-profile rounded-circle mb-2"
                                        src="http://bootdey.com/img/Content/avatar/avatar1.png" alt="">
                                    <% } %>
                                        <% } %>


                                            <div class="small font-italic text-muted mb-4">
                                                <% for( let i=0; i < 1; i++ ) { %>
                                                    <h3 style="    font-size: 20px">
                                                        <%= findAddress.Address[0].Fullname %>
                                                    </h3>
                                                    <h3 style="    font-size: 15px">
                                                        <%= findAddress.Address[0].houseaddress %>
                                                    </h3>
                                                    <h3 style="    font-size: 15px">
                                                        <%= findAddress.Address[0].State %>
                                                    </h3>
                                                    <% } %>

                                            </div>
                                            <!-- Profile picture upload button-->
                                            <div>
                                                <h3>
                                                    Wallet amount
                                                </h3>
                                                <h4>
                                                   <%= wallet %>  
                                                </h4>
                                            </div>

                    </div>
                </div>
                <a href="/profile" class="genric-btn info" style="margin-top: 8px;">Add Address</a>
            </div>

            <div class="card col-xl-9" style="margin-top: 12px;">
                <% for( let i=0; i < 1; i++ ) { %>

                    <h4 style="    margin-top: 40px;"> FullName : <%= findAddress.Address[0].Fullname %>
                    </h4>
                    <h4 style="    margin-top: 40px;"> Number : <%= findAddress.Address[0].number %>
                    </h4>
                    <h4 style="    margin-top: 40px;">House Address : <%= findAddress.Address[0].houseaddress %>
                    </h4>
                    <h4 style="    margin-top: 40px;"> RoadAddress : <%= findAddress.Address[0].roadaddress %>
                    </h4>


                    <br>

                    <% } %>

            </div>
            <div>
                <% if (deliteConfirm.length>0) { %>
                    <div class="alert alert-danger" role="alert">
                        <p class="text-danger">
                            <%= deliteConfirm %>
                        </p>
                    </div>
                    <% } %>

                        <h3 style="margin-top: 20px;">Your Address</h3>

                        <% for( let i=0; i < findAddress.Address.length; i++ ) { %>



                            <p style="margin-top: 20px; ">
                                <a style="    color: black;
            background: white;
            border: 0;
            width: 100%; 
            text-align: left;
            border-radius: unset;" class="btn btn-primary" data-bs-toggle="collapse" href="#collapseExample"
                                    role="button" aria-expanded="false" aria-controls="collapseExample">
                                    <%= findAddress.Address[i].Fullname %> <span
                                            style="font-size: 10px;     color: orange;">click here </span>
                                </a>
                                <!-- <button class="btn btn-primary" type="button" data-bs-toggle="collapse" data-bs-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
              Button with data-bs-target
            </button> -->

                            </p>
                            <div class="collapse" id="collapseExample">
                                <div class="card card-body">
                                    <%= findAddress.Address[i].Fullname %> <br>
                                        <%= findAddress.Address[i].number %><br>
                                            <%= findAddress.Address[i].houseaddress %><br>
                                                <%= findAddress.Address[i].roadaddress %>

                                                    <a href="/EditProfile/<%= findAddress.Address[i]._id %>"
                                                        class="genric-btn success" style="width: 103px;">Edit</a>
                                                    <button class="genric-btn danger" style="width: 103px;"
                                                        onclick="deleteAddress('<%= findAddress.Address[i]._id %>')">Delete</button>
                                                  
                                                    <!-- Modal -->

                                                    <!-- <a href="/deliteProfile/%= findAddress[0].Address[i]._id %>" class="genric-btn danger" style="width: 103px;">Edit</a> -->

                                </div>
                            </div>
                            <% } %>

            </div>
        </div>
    </div>
    <!-- Button trigger modal -->
    <!-- <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal">
    Launch demo modal
  </button> -->

    <!-- Modal -->
   
    <%- include('userpartials/userfooter.ejs') %>
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>

        <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
        <script>
            async function deleteAddress(addressId) {
                console.log("sss")
                Swal.fire({
                    title: 'Are you sure?',
                    text: "You won't be able to revert this!",
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#3085d6',
                    cancelButtonColor: '#d33',
                    confirmButtonText: 'Yes, delete it!'
                }).then(async (result) => {
                    if (result.isConfirmed) {
                        await axios.delete(`/deliteProfile?addressId=${addressId}`)
                            .then((ans) => {
                                if (ans.data) {
                                    Swal.fire(
                                        'Deleted!',
                                        'Your file has been deleted.',
                                        'success'
                                    ).then((result) => {
                                        location.reload()
                                    })
                                        .catch((err) => {
                                            alert("somthing Went wrong")
                                        })
                                }

                            })
                    }
                })
                // await axios.delete(`/deliteProfile?addressId=${addressId}`)
                //     .then((ans) => {
                //         console.log(ans)
                //         location.reload()
                //     })
            }
        </script>

        <script>
            var myModal = document.getElementById('myModal')
            var myInput = document.getElementById('myInput')

            myModal.addEventListener('shown.bs.modal', function () {
                myInput.focus()
            })
        </script>



        <style type="text/css">
            body {
                margin-top: 20px;
                background-color: #f2f6fc;
                color: #69707a;
            }

            .img-account-profile {
                height: 10rem;
            }

            .rounded-circle {
                border-radius: 50% !important;
            }

            .card {
                box-shadow: 0 0.15rem 1.75rem 0 rgb(33 40 50 / 15%);
            }

            .card .card-header {
                font-weight: 500;
            }

            .card-header:first-child {
                border-radius: 0.35rem 0.35rem 0 0;
            }

            .card-header {
                padding: 1rem 1.35rem;
                margin-bottom: 0;
                background-color: rgba(33, 40, 50, 0.03);
                border-bottom: 1px solid rgba(33, 40, 50, 0.125);
            }

            .form-control,
            .dataTable-input {
                display: block;
                width: 100%;
                padding: 0.875rem 1.125rem;
                font-size: 0.875rem;
                font-weight: 400;
                line-height: 1;
                color: #69707a;
                background-color: #fff;
                background-clip: padding-box;
                border: 1px solid #c5ccd6;
                -webkit-appearance: none;
                -moz-appearance: none;
                appearance: none;
                border-radius: 0.35rem;
                transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
            }

            .nav-borders .nav-link.active {
                color: #0061f2;
                border-bottom-color: #0061f2;
            }

            .nav-borders .nav-link {
                color: #69707a;
                border-bottom-width: 0.125rem;
                border-bottom-style: solid;
                border-bottom-color: transparent;
                padding-top: 0.5rem;
                padding-bottom: 0.5rem;
                padding-left: 0;
                padding-right: 0;
                margin-left: 1rem;
                margin-right: 1rem;
            }
        </style>

        <script type="text/javascript">

        </script>